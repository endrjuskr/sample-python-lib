from setuptools import setup, find_packages

setup(name='sample',
      version='0.2.1',
      description='A sample project',
      install_requires=['numpy>=1.18.2'],
      zip_safe=False,
      python_requires='>=3.6'
)